export default function () {
  return [
    { title: 'javascript', pages: Math.round(Math.random()*100)},
    { title: 'harry potter', pages: Math.round(Math.random()*100)},
    { title: 'GOT', pages: Math.round(Math.random()*100)},
    { title: 'dark tower', pages: Math.round(Math.random()*100)}
  ]
}